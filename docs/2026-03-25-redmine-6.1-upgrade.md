# Vault Plugin — Redmine 6.1 Upgrade Plan

> **For agentic workers:** Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Patch vault plugin v0.4.3 for compatibility with Redmine 6.1 (Rails 7.2, Ruby 3.2+)

**Architecture:** Mechanical migration — remove deprecated Rails/Ruby APIs, replace with modern equivalents. No functional changes.

**Tech Stack:** Ruby 3.2+, Rails 7.2, Redmine 6.1

---

### Task 1: Remove `attr_accessible` (removed in Rails 4+, replaced by strong params)

**Files:**
- Modify: `app/models/vault/key.rb`
- Modify: `app/models/vault/tag.rb`
- Modify: `app/models/vault/key_file.rb`

- [ ] Remove `attr_accessible` lines from all models (strong params already handled in controllers via `key_params`)

### Task 2: Remove `unloadable` (removed in Rails 5+)

**Files:**
- Modify: `app/models/vault/key.rb`
- Modify: `app/models/vault/tag.rb`
- Modify: `app/controllers/keys_controller.rb`
- Modify: `app/controllers/key_files_controller.rb`
- Modify: `app/controllers/vault_settings_controller.rb`
- Modify: `app/controllers/vault_context_menus_controller.rb`

- [ ] Remove all `unloadable` lines

### Task 3: Remove `require 'iconv'` (removed in Ruby 2.0+)

**Files:**
- Modify: `app/models/vault/key.rb`

- [ ] Remove `require 'iconv'` line

### Task 4: Replace `update_attributes` → `update` (deprecated Rails 6, removed Rails 7)

**Files:**
- Modify: `app/controllers/keys_controller.rb`

- [ ] Change `@key.update_attributes(params[:vault_key])` → `@key.update(params[:vault_key])`

### Task 5: Replace `Dir.exists?` → `Dir.exist?` (removed Ruby 3.2)

**Files:**
- Modify: `lib/mk_keyfiles_dir.rb`

- [ ] Change `Dir.exists?` → `Dir.exist?`

### Task 6: Replace `require_dependency` → `require_relative` (deprecated Rails 7)

**Files:**
- Modify: `init.rb`
- Modify: `lib/project_patch.rb`

- [ ] Replace all `require_dependency` with `require` or `require_relative`

### Task 7: Replace monkey-patching `send(:include)` → `prepend`

**Files:**
- Modify: `lib/project_patch.rb`

- [ ] Rewrite ProjectPatch to use `Project.prepend(ProjectPatch)` instead of `Project.send(:include, ProjectPatch)`

### Task 8: Fix migrations for Rails 7.2

**Files:**
- Modify: all `db/migrate/*.rb`

- [ ] Remove dual Redmine version checks, use `ActiveRecord::Migration[4.2]` unconditionally (Redmine 6.1 is always Rails 7.2)

### Task 9: Update plugin version

**Files:**
- Modify: `init.rb`

- [ ] Bump version to `0.5.0`
- [ ] Update `requires_redmine` to `version_or_higher => '6.0'`
